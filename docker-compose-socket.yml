version: "3"
services:
  socket-client:
    build:
      context: ./services/sockert_client
      dockerfile: ${DOCKER_FILE:-Dockerfile}
    stdin_open: true # docker run -i
    tty: true # docker run -t
    command: /bin/sh
    volumes:
      - ./cli:/usr/src/app
    env_file:
      - ./.env
