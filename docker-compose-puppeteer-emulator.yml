version: "3"
services:
  puppeteer-emulator:
    build:
      context: ./services/puppeteer_emulator
      dockerfile: ${DOCKER_FILE:-Dockerfile}
    stdin_open: true # docker run -i
    tty: true # docker run -t
    command: /bin/sh
    volumes:
      - ./services/puppeteer_emulator:/usr/src/app
    env_file:
      - ./.env
